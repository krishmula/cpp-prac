// We have a string. We want to find out, for each prefix of the string, how many divisions of the prefix can we make, such that each resultant string is an anagram.

#include <iostream>
#include <vector>
#include <algorithm>
#include <string>


using namespace std;

int getDivisions(string s) {

}

int main() {
    string s = "ABBA";
    int result = getDivisions(s);
}